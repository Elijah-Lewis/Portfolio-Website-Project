import { Typography, AppBar } from '@mui/material';
import Button from '@mui/material/Button';

export default function App() {
  
  return <> 
  <AppBar></AppBar>
  <Button variant="contained">Contained</Button> 
  <Typography>TestText</Typography>
  </>
}